import type {NormalizedTrip} from "@/types/trip";import {ruleSnapshots} from "./fixtures";export type PlanningConflict={key:string;severity:"warning"|"blocking";title:string;reason:string;recommendedAction:string;dayId:string;sourceUrl:string;verifiedAt:string};export function evaluateTrip(t:NormalizedTrip){const out:PlanningConflict[]=[];const add=(key:string,dayId:string,severity:"warning"|"blocking",title:string,reason:string,recommendedAction:string)=>{const r=ruleSnapshots.find(x=>x.key===key)!;out.push({key,severity,title,reason,recommendedAction,dayId,sourceUrl:r.sourceUrl,verifiedAt:r.verifiedAt});};t.days.forEach(d=>{if(d.events.some(e=>e.title.toLowerCase().includes("water park")))add("water-park-benefit",d.id,"blocking","Water-park benefit ends before check-in","The published benefit ends September 8; this resort check-in is September 19.","Keep the activity with separate admission or choose a resort afternoon.");if(d.events.some(e=>e.title.includes("Space 220")))add("space-220-dining",d.id,"warning","Space 220 is out of pocket","The imported plan says this meal does not accept the dining plan.","Keep it out of pocket and reserve separately.");const group1=d.lightningLane.filter(x=>x.passType==="multi"&&x.group===1);if(d.parks.includes("EPCOT")&&group1.length>1)add("epcot-multipass",d.id,"blocking","EPCOT Group 1 conflict","Frozen Ever After and Remy's Ratatouille Adventure are both Group 1 initial choices.","Choose one for the initial set and preserve the other as an after-redemption wish.");if(d.parks.some(p=>p.includes("Hollywood"))&&group1.length>1)add("hollywood-studios-multipass",d.id,"warning","Hollywood Studios wish count needs a Group 1 choice","More than one Group 1 wish cannot be in the initial set.","Choose one Group 1 attraction and defer the other.");if(d.events.some(e=>e.type==="flight")&&d.parks.length)add("flight-buffer",d.id,"warning","Flight-day park buffer is tight","A Magic Kingdom idea appears before the 5:13 PM MCO flight.","Set a leave-for-airport time before choosing park plans.");});return out;}
